// Github/gitlab/bitbucket - Version Control Tool

// To work with github/bitbucket/gitlab, we need to have "git" installed in our local
// We can download git from https://git-scm.com/install/windows and install in local

// git - Tool used to track the code changes
// github - Online Platform where git repository are stored and helps us in terms 
// of collaboration


// 1. You are the first employee to start test automation - NO REPO AVAILBLE

    // 0. Create a new repository on the version control tool
    // 1. Create a project and write the test cases inside your local system
    // 2. "git init" - Initialize the a new got repo inside your local system
    // 3. "git add ." - Add all the files that you have changed/added in your
    // local git area
    // 4. "git commit -m "first commit" " - Commit all the changes to your local
    // git with the message
    // 5. "git branch -M master" - Create a new branch named as "master"
    // 6. "git branch" - List all the branches that in your local repo
    // 7. "git remote add origin https://github.com/Praveenroy05/gitdemo.git" - Add
    // a remote repository to your local repo and link it with the remote repo
    // 8. "git push -u origin master" - Push the commited changes from local "master"
    // branch to remote repository inside the master branch

    // 9. "git checkout master" - Move to the master branch and pull the latest changes
    // 10. "git pull" - It will pull all the latest changes from the remote repo tp
    // local repo

    // 11. git checkout -b branchName - Create a new branch going forward for all the new task
    // 12 . Complete writing the test case and follow the steps from #3, #4 and #8(change the branch name)

    // follow #9 - #12




// 2. If the Repository is already available

    // 0. "git clone repoURL" - Clone the repository in your local system
    // 1. "npm install" - Install all the dependencies
    // 2. "git checkout -b featureBrach" - Create a new brach for all the task going foward
    // 3. "git branch" - List out all the branches that are availble in your local system
    // 4. Write the test case in your local system save all the files
<<<<<<< HEAD
    // 5. "git status" - Validate al the files that you have changed/added/delete
    // 6. "git add ." - Add all the changed files to your local git area
    // 7.  "git commit -m "message" " - Commit all the changes to your local
=======
    // 5. "git status" - Validate all the files that you have changed/added/delete
    // 6. "git add ." - Add all the changed files to your local git area
    // 7. "git commit -m "message" " - Commit all the changes to your local
>>>>>>> f8e9333 (added git concepts)
    // git with the message
        // 7.1. git checkout master - Switch to master branch
        // 7.2. git pull - Pull all the changes from your remote master 
        // branch to your local master branch
        // 7.3. git checkout featureBranch - Switch to the feature branch
        // 7.4 . git rebase --continue  - use this only if you get the conflict
        // and you have resolved the conflict in your local
    
    // 8. git push -u origin featurebranch - Push the committed changes 
    // from the featurebranch to the remote feature
    // 9. Goto github/bitbucket/gitlab and create a Pull Request (PR) to
    // merge your changes to the master branch

    // Follow #2 - #9


